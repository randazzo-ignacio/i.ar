1. `(setq my-gptel--guard-allow-self-modification nil)` -- lock down self-modification
2. `(setq-local my-gptel--sanitize-exec-output t)` -- enable output sanitizer in the ctfwizard session
3. Edit `agents.d/ctfwizard/prompt.org` to insert the CTF rules and scope

