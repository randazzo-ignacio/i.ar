---
# inventory/hosts.yml
# ────────────────────────────────────────────────────────────
# 5 hosts: 3 cloud VPS + 2 local machines.
# Groups: cloud, local, ollama_hosts, proxy, ai_playground, frigate_hosts.
# ────────────────────────────────────────────────────────────

all:
  children:
    cloud:
      hosts:
        daftpunk:
          ansible_user: root
        rammstein:
          ansible_user: root
        greenday:
          ansible_user: root
    local:
      hosts:
        yoga:
          ansible_user: root
        sophon:
          ansible_user: root

    # ── Functional groups (hosts can belong to multiple) ────
    proxy:
      hosts:
        rammstein:
    ai_playground:
      hosts:
        greenday:
    ollama_hosts:
      hosts:
        daftpunk:
        sophon:
    frigate_hosts:
      hosts:
        sophon:
